// Mapa central de ícones — StorePro
// Fonte única: Lucide React. Outline, stroke-width 2 por padrão.
// Importe daqui em vez de lucide-react diretamente para manter consistência.

export {
  // ── Módulos / Navegação ─────────────────────────────────────
  Calendar,       // Agenda
  Receipt,        // Comanda / ticket
  Banknote,       // Caixa / dinheiro em espécie
  Scissors,       // Serviços / barbeiro
  Users,          // Equipe (plural)
  User,           // Cliente (singular)
  Star,           // Planos / assinatura
  TrendingUp,     // Financeiro
  BarChart2,      // Relatórios
  Package,        // Estoque
  Store,          // Marca / App / Logo

  // ── Categorias operacionais ─────────────────────────────────
  Beer,           // Bar / bebidas
  ShoppingBag,    // Loja / produtos varejo
  CreditCard,     // Cartão (débito / crédito)
  QrCode,         // Pix
  Wallet,         // Saldo / carteira

  // ── Ações ───────────────────────────────────────────────────
  Plus,           // Adicionar
  Pencil,         // Editar
  Trash2,         // Excluir
  Check,          // Confirmar (inline)
  CheckCircle2,   // Concluído (estado)
  X,              // Fechar modal / cancelar
  XCircle,        // Erro / cancelado
  Search,         // Buscar / filtrar
  RefreshCw,      // Recarregar / atualizar
  Lock,           // Acesso restrito / senha

  // ── Status / Indicadores ────────────────────────────────────
  AlertTriangle,  // Alerta / atenção
  Clock,          // Horário / tempo
  AlarmClock,     // Horário de fechamento
  Moon,           // Fora do horário / noturno
  Zap,            // Sugestão rápida / destaque
  Crown,          // VIP / assinante
  Trophy,         // Melhor / ranking / top
  Lightbulb,      // Insights / inteligente
  ArrowUpRight,   // Crescimento / positivo
  ArrowDownRight, // Queda / negativo
  TrendingDown,   // Queda em série
  Repeat2,        // Fidelização / recorrente
  RotateCcw,      // Reverter / historico

  // ── KPIs / Dados ────────────────────────────────────────────
  DollarSign,     // Faturamento / monetário
  Hash,           // Contagem / quantidade
  Percent,        // Porcentagem / taxa
  Gem,            // Premium / destaque especial

  // ── Estados vazios ──────────────────────────────────────────
  CalendarOff,    // Sem agendamentos
  PackageX,       // Sem estoque
  UserX,          // Cliente inativo / ausente
  Inbox,          // Lista vazia / sem dados

  // ── App / Sobre ─────────────────────────────────────────────
  Smartphone,     // PWA / mobile
  Sparkles,       // Novidades / highlights
  Bot,            // IA / automação
  Bell,           // Notificações / alertas

  // ── Layout ──────────────────────────────────────────────────
  ChevronDown,
  ChevronRight,
  ChevronLeft,
  ChevronUp,
  Home,
} from "lucide-react";
